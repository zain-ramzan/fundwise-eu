import DashboardLayout from "@/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { formatDate } from "@/lib/funding";
import { trpc } from "@/lib/trpc";
import { Bell, Bookmark, CalendarDays, ChevronRight, ClipboardList, Loader2, Plus } from "lucide-react";
import { Link } from "wouter";

function MetricCard({ icon: Icon, label, value, description }: { icon: typeof Bookmark; label: string; value: number; description: string }) {
  return <div className="rounded-2xl border border-[#dbe6df] bg-white p-5 shadow-[0_8px_25px_rgba(20,61,51,0.04)]"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#edf5ed] text-[#4d796c]"><Icon className="h-5 w-5" /></span><p className="mt-5 text-sm font-semibold text-[#506860]">{label}</p><p className="mt-1 font-display text-4xl text-[#1c493f]">{value}</p><p className="mt-2 text-xs leading-5 text-[#778981]">{description}</p></div>;
}

export default function UserDashboard() {
  const { data, isLoading } = trpc.dashboard.overview.useQuery();
  if (isLoading) return <DashboardLayout><div className="grid min-h-[60vh] place-items-center"><Loader2 className="h-6 w-6 animate-spin text-[#48786a]" /></div></DashboardLayout>;
  return <DashboardLayout><div className="mx-auto max-w-6xl"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-[#6b887d]">Your funding workspace</p><h1 className="mt-2 font-display text-4xl tracking-[-0.04em] text-[#17443c]">A clear view of what matters.</h1><p className="mt-2 text-sm leading-6 text-[#677a72]">Monitor your saved opportunities, application activity and approaching deadlines.</p></div><Button asChild className="rounded-full bg-[#0f3d36] text-white"><Link href="/funding"><Plus className="mr-2 h-4 w-4" />Discover funding</Link></Button></div><div className="mt-8 grid gap-4 sm:grid-cols-3"><MetricCard icon={Bookmark} label="Saved opportunities" value={data?.savedCount ?? 0} description="Your short-list of records to revisit." /><MetricCard icon={ClipboardList} label="Active applications" value={data?.activeApplications ?? 0} description="Draft, in-progress and submitted work." /><MetricCard icon={CalendarDays} label="Approaching deadlines" value={data?.upcomingDeadlines.length ?? 0} description="Saved opportunities closing within view." /></div><div className="mt-8 grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><section className="rounded-2xl border border-[#dbe6df] bg-white p-5 sm:p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-[#728b81]">Application flow</p><h2 className="mt-1 font-display text-2xl text-[#1b483f]">Where your work stands</h2></div><Button asChild variant="ghost" className="rounded-full text-[#316656]"><Link href="/applications">View all <ChevronRight className="ml-1 h-4 w-4" /></Link></Button></div><div className="mt-6 grid grid-cols-5 gap-2">{Object.entries(data?.statusCounts ?? {}).map(([status,count]) => <div key={status} className="rounded-xl bg-[#f3f7f3] p-3"><p className="font-display text-2xl text-[#234d43]">{count}</p><p className="mt-1 text-[10px] font-semibold leading-4 text-[#71837b]">{status}</p></div>)}</div><div className="mt-7"><Link href="/applications" className="group flex items-center justify-between rounded-xl border border-dashed border-[#c7d8ce] p-4 text-sm text-[#557067] hover:border-[#83a99a] hover:bg-[#f6faf6]"><span>Open the application workspace to manage documents and milestones.</span><ChevronRight className="h-4 w-4 transition-transform group-hover:translate-x-0.5" /></Link></div></section><section className="rounded-2xl border border-[#dbe6df] bg-[#f5f8f5] p-5 sm:p-6"><div className="flex items-center gap-2"><Bell className="h-4 w-4 text-[#5c8476]" /><h2 className="font-display text-2xl text-[#1b483f]">Recent reminders</h2></div><div className="mt-5 space-y-3">{data?.notifications.length ? data.notifications.map(notification => <div key={notification.id} className="rounded-xl bg-white p-3.5"><p className="text-sm font-semibold text-[#335a4f]">{notification.title}</p><p className="mt-1 text-xs leading-5 text-[#70827b]">{notification.message}</p>{notification.dueAt && <p className="mt-2 text-xs font-semibold text-[#668477]">Due {formatDate(notification.dueAt)}</p>}</div>) : <p className="rounded-xl bg-white p-4 text-sm leading-6 text-[#71847c]">No reminders yet. Save an opportunity or create an application to keep deadline activity visible here.</p>}</div></section></div><section className="mt-8 rounded-2xl border border-[#dbe6df] bg-white p-5 sm:p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-[#728b81]">Upcoming from your short-list</p><h2 className="mt-1 font-display text-2xl text-[#1b483f]">Deadlines to keep in view</h2></div><Button asChild variant="ghost" className="rounded-full text-[#316656]"><Link href="/funding">Browse catalogue</Link></Button></div><div className="mt-5 grid gap-3">{data?.upcomingDeadlines.length ? data.upcomingDeadlines.map(item => <Link href={`/funding/${item.opportunity.slug}`} key={item.opportunity.id} className="flex flex-col justify-between gap-3 rounded-xl border border-[#e0eae3] p-4 transition hover:border-[#abc4b7] hover:bg-[#fbfdfb] sm:flex-row sm:items-center"><div><p className="text-sm font-semibold text-[#285347]">{item.opportunity.title}</p><p className="mt-1 text-xs text-[#71837b]">{item.opportunity.programme}</p></div><p className="shrink-0 text-sm font-semibold text-[#2f6656]">{formatDate(item.opportunity.deadlineAt)}</p></Link>) : <p className="text-sm leading-6 text-[#71847c]">Your saved opportunity deadlines will appear here.</p>}</div></section></div></DashboardLayout>;
}
